## Copy this file to `.env` and fill values.
# Authentication token support removed. Protect the service via network or reverse proxy.
# Default model served by the gateway
LITERT_MODEL=gemma-4-E2B-it.litertlm
